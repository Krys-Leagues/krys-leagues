"use client"
/* eslint-disable react-hooks/set-state-in-effect */
import { useCallback,useEffect,useState } from "react"
import { useRouter } from "next/navigation"
import { supabase } from "@/lib/supabase"
type Week={week_number:number;course_name:string|null;status:"open"|"closed";due_date:string|null};type Season={season_number:number;league_type:string|null}
export default function SoloWeeksPage(){const router=useRouter();const [seasonId,setSeasonId]=useState("");const [season,setSeason]=useState<Season|null>(null);const [weeks,setWeeks]=useState<Week[]>([]);const [message,setMessage]=useState("");const [loading,setLoading]=useState(true);const [busy,setBusy]=useState(0);const load=useCallback(async()=>{const id=new URLSearchParams(window.location.search).get("seasonId")||"";setSeasonId(id);if(!id){setMessage("A seasonId is required. No fallback season was loaded.");setLoading(false);return}const [{data:s,error:se},{data:w,error:we}]=await Promise.all([supabase.from("seasons").select("season_number,league_type").eq("id",id).maybeSingle(),supabase.from("solo_weeks").select("week_number,course_name,status,due_date").eq("season_id",id).order("week_number")]);if(se||we||!s||s.league_type!=="solo"||(w||[]).length!==4){setMessage(se?.message||we?.message||"The exact managed Solo season and its four weeks were not found.");setLoading(false);return}setSeason(s as Season);setWeeks((w||[])as Week[]);setLoading(false)},[]);useEffect(()=>{void load()},[load]);function patchWeek(n:number,change:Partial<Week>){setWeeks(v=>v.map(w=>w.week_number===n?{...w,...change}:w))}async function save(w:Week){setBusy(w.week_number);const {error}=await supabase.rpc("update_solo_week",{p_season_id:seasonId,p_week_number:w.week_number,p_course_name:w.course_name||null,p_due_date:w.due_date||null});setBusy(0);setMessage(error?.message||`Week ${w.week_number} saved.`);if(!error)await load()}return <main style={page}><div style={container}><div style={nav}><button style={button} onClick={()=>router.push(`/admin/solo/setup?seasonId=${encodeURIComponent(seasonId)}`)}>← Setup / Roster</button><button style={button} onClick={()=>router.push(`/admin/solo?seasonId=${encodeURIComponent(seasonId)}`)}>Solo Hub</button></div><h1>Solo Weeks</h1><p style={muted}>{season?`Season ${season.season_number} · exactly four independent weeks`:"Load the exact managed Solo season."}</p>{loading?<p>Loading…</p>:<div style={grid}>{weeks.map(w=><section key={w.week_number} style={card}><h2>Week {w.week_number}</h2><p>Status: <strong>{w.status}</strong></p><label style={field}>Map / course<input style={input} value={w.course_name||""} disabled={w.status!=="open"} onChange={e=>patchWeek(w.week_number,{course_name:e.target.value})}/></label><label style={field}>Due date<input type="date" style={input} value={w.due_date||""} disabled={w.status!=="open"} onChange={e=>patchWeek(w.week_number,{due_date:e.target.value})}/></label><button style={primary} disabled={busy>0||w.status!=="open"} onClick={()=>save(w)}>{busy===w.week_number?"Saving…":"Save week"}</button></section>)}</div>}{message&&<p style={notice}>{message}</p>}<p style={muted}>Easy/Hard results, recognition, ranking, and Close Week are intentionally deferred to Batch 2.</p></div></main>}
const page:React.CSSProperties={minHeight:"100vh",padding:24,background:"black",color:"white"};const container:React.CSSProperties={maxWidth:1000,margin:"0 auto"};const nav:React.CSSProperties={display:"flex",justifyContent:"space-between",gap:10};const muted:React.CSSProperties={color:"#bbb"};const grid:React.CSSProperties={display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:14};const card:React.CSSProperties={padding:18,border:"1px solid #333",borderRadius:12,background:"#0d0d0d"};const field:React.CSSProperties={display:"flex",flexDirection:"column",gap:7,margin:"14px 0",fontWeight:700};const input:React.CSSProperties={padding:10,border:"1px solid #444",borderRadius:8,background:"#111",color:"white"};const button:React.CSSProperties={padding:"9px 13px",border:"1px solid #555",borderRadius:8,background:"#171717",color:"white"};const primary:React.CSSProperties={...button,background:"#126b3c",borderColor:"#167a45",fontWeight:800};const notice:React.CSSProperties={padding:12,border:"1px solid #444",borderRadius:8,background:"#171717"}
